pub fn add1(a: usize, b: usize) -> usize {
    a + b
}

pub fn add2(a: usize, b: usize) -> usize {
    let num = a + b;
    num
}

pub fn add3(a: usize, b: usize) -> usize {
    let num = a + b;
    return num;
}
